<div class="card text-center">
  <div class="card-body">
    <h5 class="card-title">ĐĂNG NHẬP VÀO TÀI KHOẢN CỦA BẠN</h5>
    <form method="POST" action="/login" id="login_form" onsubmit="saveToken()">
        <div class="form-group row">
            <label for="inputEmail3" class="col-sm-2 col-form-label">Tài khoản của bạn</label>
            <div class="col-sm-10">
            <input name="user_name" type="text" class="form-control" id="inputEmail3" placeholder="Bạn có thể nhập tên tài khoản hoặc gmail">
            </div>
        </div>
        <div class="form-group row">
            <label for="inputPassword3" class="col-sm-2 col-form-label">Password</label>
            <div class="col-sm-10">
            <input name="pass_word" type="password" class="form-control" id="inputPassword3" placeholder="Password">
            </div>
        </div>
        <div class="form-group row">
            <div class="col-sm-10">
            <button type="submit" class="btn btn-primary mt-4 login-btn">Sign in</button>
            <div class="mt-2">
                <a href="http://localhost:3000/register" class="text-success">Bạn chưa có tài khoản? Đăng kí ngay</a>
            </div>
            </div>
        </div>
    </form>
  </div>
</div>

<script>
  async function saveToken(){
    console.log("Saving token")
    const response = await fetch('http://localhost:3000/login', {
        method: 'POST',
        headers: {
        'Content-Type': 'text/plain'
        },
        body: data
    });
    const token = await response.json();
    return console.log(token)
  }

</script>